package Random is
   --  function that generates random numbers between a lower and an upper bound
   procedure generateRandom (LowerBound, UpperBound : in Positive ;
                             Result : out Positive);
end Random;
